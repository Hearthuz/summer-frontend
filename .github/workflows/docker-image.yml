name: Docker Image CI
run-name: ${{ github.actor }} is pushing
on: [push]
jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build docker image
        runs: docker build -t hearthuz/summer-frontend:latest .
